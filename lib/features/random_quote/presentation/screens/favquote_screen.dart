import 'package:flutter/material.dart';

class FavQuoteScreen extends StatelessWidget {
  const FavQuoteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('helo'),
    );
  }
}